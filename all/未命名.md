```mermaid
graph UD
    subgraph 剪枝搜索
        A[开始] --> B{当前节点是否为目标节点?}
        B -- 是 --> C[返回当前节点]
        B -- 否 --> D{当前节点是否已被访问?}
        D -- 是 --> C[返回当前节点]
        D -- 否 --> E{当前节点是否可行?}
        E -- 是 --> F[标记当前节点为已访问]
        E -- 否 --> C[返回当前节点]
        F --> G{生成当前节点的子节点}
        G --> H{选择最佳子节点}
        H --> I{递归调用剪枝搜索算法}
        I --> C[返回剪枝搜索结果]
    end
```